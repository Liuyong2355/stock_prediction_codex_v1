# Reference Sources

## Present

- `赛题五-更新.pdf` — organizer task statement.

## Required but not yet present

- Organizer-provided `evaluate.py`.

The competition PDF states that a Python evaluation implementation is supplied. It has not been added to this package yet.

When it becomes available:

1. place it at `reference/evaluate_official.py`;
2. do not modify that file;
3. compare any internal evaluator against it on deterministic fixtures;
4. if behavior differs, the organizer evaluator wins;
5. update `docs/COMPETITION_SPEC.md` only if an edge case becomes concretely resolved.
