# E006 / E007 验证记录

## 接续与冻结条件

- 接续时实际 HEAD 为 `24e979d361516af6d3d85de08e4d6b3e3be5b97b`，工作区干净。
- 读取 AGENTS.md、docs/index、实验协议、任务定义、决策、Full147 实现/审计、target 对照报告及全部冻结模型/fold 配置后实施。
- 本阶段仅 E006/E007；Full147 矩阵、147 列顺序、F1–F3、一天 purge、target/relevance 和模型参数全部沿用冻结定义。
- 原有特征、切分、target、E003/E004/E005 训练与评分源文件保持字节不变。新入口复用已有函数，核心训练分别直接调用官方 XGBRegressor 和 LGBMRanker。
- 官方脚本 SHA-256：`72dc59987e92ba8ae6b506e114dfe8591b5608c9beccd0769d5d1f778f9189c2`。

## 环境

在观察 E006 评分前固定新增官方发行版 `xgboost==3.4.1`；其余依赖不变，LightGBM 仍为 4.6.0。
安装使用官方发行 wheel，不更新原有依赖；`pip check` 通过，完整清单见 `e006_e007_environment_lock.txt`。
模型及 ranking group API 依据本地安装的官方库签名/docstring 核验。

XGBoost 使用其原生 hist/QuantileDMatrix 路径，内部精度与缺失处理由官方库负责。
没有自行实现树、梯度、排序损失、优化器或模型训练算法。

## 训练前测试

- 新增 9 项 group/relevance/官方库冒烟测试通过，完整输出见 `e006_e007_smoke_tests.txt`。
- 正式训练启动前，全量 **204 项测试通过**，保留输出见 `e006_e007_pretraining_tests.txt`。
- 两类模型都以冻结 147 列、800 轮完成小样本重复拟合，预测逐值一致；joblib 与原生 UBJSON/文本模型预测一致。
- 覆盖并列值、常数日期、单股票日期、decile 边界、0–9 relevance、稳定日期排序、X/y 行序、group 大小合计与单日期连续性。
- 覆盖原始训练切分/purge、非有限标签拒绝、未来/验证标签扰动不改变训练计划，以及实际入口只向构造器传入监督训练切片。
- 实际模型输出直接调用官方脚本，与逐日诊断指标核对，缺失标签保留。
- 比较程序另有独立 NumPy/SciPy oracle、未定义折不能选择性求均值、Score 归因加总测试。

E007/F1 的负 IC 另做了全量只读核验：3,568,878 个 relevance、拟合行索引以及 972 个 group 日期/大小均与独立 NumPy/SciPy 构造逐字节一致。
0–9 relevance 分布正常，各桶约 35.7 万行；Top 缺失标签占比为 86.33%。
证据见 `e006_e007_ranker_f1_group_check.json`。未因该结果变更参数、分组、relevance 或预测。

## 跨目录文本指纹复现

历史 Git LF 与工作区 CRLF 差异沿用严格按字节冻结的合同，不更改既有指纹。
`restore_model_text_contract` 默认只检查；仅在明确请求恢复时，才写入能够精确命中既有 SHA-256 的 LF/CRLF 变体。
真实内容变化会在任何写入之前拒绝。大文件/模型/预测不参与该工具的修改。

隔离副本实测：96 个受检文本文件全部匹配；LF 模拟检出中 71 个文件恢复到历史接受字节。
原始工作区未修改，证据见 `e006_e007_text_roundtrip.json`。

## 最终测试

完整测试集 **207 项全部通过，耗时 18.21 秒**；输出见 `e006_e007_test_results.txt`。
包括既有特征因果性、切分/purge、target 和官方评分测试，以及本阶段新增的 group/relevance、实际官方库重复拟合、模型序列化和比较统计测试。

## 全量产物与官方评分核验

`python -m stock_prediction.compare_models` 正常退出，完整日志见 `e006_e007_reproduction.log`。

- E000–E007 共 **24 组**保存预测直接重放原始官方脚本；全部指标与对应官方记录一致，绝对容差 1e-14，E000 的未定义 IC/Score 原样保留。
- 六个新模型重载后，**6,751,800 条**验证预测逐值完全相同；每个原生 UBJSON/文本模型另取固定 257 条样本核验，同样完全相同。
- 六折的完整监督标签与拟合行序通过独立 NumPy/SciPy 重构和指纹比对；E006 Rank 标签和行序与 E004 完全一致。
- E007 的 F1/F2/F3 分别为 **972 / 1,214 / 1,456 个连续日期组**，大小合计分别为 **3,568,878 / 4,597,785 / 5,659,954**；group 文件、日期、大小及 relevance 全部匹配。
- 六折验证键、原始标签、涨停标记与 E004 相同；Full147、冻结配置、旧训练/评分代码、E000–E005 产物均通过原指纹核验。
- 原日志 18 行的字节前缀保持不变，只追加本阶段 6 行，共 24 行且无重复。
- 每折缺失标签 Top 占比和剔除缺失标签后的诊断换手另行复算一致；不改变预测、样本或官方 Score。

完整机器可读核验标记见 `e006_e007_comparison.json` 的 `verification`；三折指标、均值、最差折、稳定性、分项归因与 Phase C 建议见对应 Markdown 报告。

## Git 提交前核验

- 129 个受保护的既有已跟踪文件相对基线无变动。
- 本阶段 24 个已跟踪模型附属产物的暂存字节与记录的 SHA-256 一致；六份 `result.json` 的字节保持一致。
- 新训练源码的暂存内容可精确匹配训练指纹，或通过既有的 LF/CRLF 字节合同精确恢复。
- 大矩阵、模型和完整预测未被误加入 Git；文件清单与暂存文件一致，暂存差异检查通过。
- 证据见 `e006_e007_git_validation.json`。模型/预测仍保留在当前工作区供审查重放。

## 复现边界

完整正式训练不重复执行第二遍；重复拟合检验使用固定合成小样本。
正式结果复现采用保存模型重载、完整验证预测逐值比较、原生模型固定样本核验、官方脚本再次复算。
运行结果记录启动时 Git HEAD 和训练源码指纹；本阶段新增源码随结果一起提交，不声称已包含在基线提交中。
比较报告和文本恢复工具在训练期间完善，不参与训练计算。

大矩阵、joblib、UBJSON、完整预测依照既有规则本地保留；Git 提交指标、group、配置、指纹、报告和测试。
另一个环境需准备清单中的原始数据/大文件及记录路径，不能仅凭 Git clone 重训全量数据。
