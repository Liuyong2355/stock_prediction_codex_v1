# E004 / E005 验证记录

## 范围与来源

- 接续前实际仓库 HEAD：`eb9660aec0b5147cce91cc1837be2406e9bf0300`，工作区干净。
- 已读取 AGENTS.md、docs/index.md、冻结实验/特征/任务说明、决策记录、config YAML、Full147 审计/验证、E003 运行记录及 E002/E003 官方对照。
- 仅新增 E004、E005；不运行 E006/E007，不调参、融合或处理 turnover。
- 官方脚本 SHA-256：`72dc59987e92ba8ae6b506e114dfe8591b5608c9beccd0769d5d1f778f9189c2`。
- 冻结 YAML 仍保留历史 `evaluator_status: provisional` 字段；本阶段所有正式分数直接来自已获得的原始官方脚本，并在新 result.json 中明确记录实际评分来源。未改写历史说明或冻结定义。

## 自动化测试

全量测试：**195 passed**，完整输出见 `e004_e005_test_results.txt`。

新增覆盖包括：

- Raw/Rank/Relative 的手算样例、并列标签、单股票日期、常数截面、交错日期和行序保持。
- 非有限标签拒绝、purge 与缺失标签过滤；未来/验证标签变动不影响监督训练 target。
- 修改另一日期的标签不影响当前日期；Rank/Relative 的当日平移不变性。
- NumPy/SciPy 独立 target oracle 与训练实现对随机并列截面逐值相等。
- 实际运行入口只向 target 构造器传入 purge 后的有限训练行，验证标签与 purge 行不进入该调用。
- E004/E005 使用与 E003 一致的完整参数，147 列、800 轮；小规模重复拟合和模型重载预测逐值相等。
- Score 拆解符号、贡献加总、最差折方向与跨折 ddof=0 稳定性。
- E006/E007 及 rank-decile 构造被本阶段入口拒绝。
- LF/CRLF 恢复必须逐字节命中既有指纹；真实内容改动必须拒绝。

最初一个手算测试误将两股票截面的最低百分位写成 0.25；按冻结 rank_pct 的 1/2 修正测试期望为 0.5−0.5=0。生产公式未调整。正式训练在相关 14 项冒烟测试通过后启动。

## 正式产物核验：全部通过

- 六折均完成固定 800 轮，旧实验日志逐字节前缀保留，总计 18 行实验记录、仅追加六行，无重复实验/折。
- E003/E004/E005 九组保存预测再次直接调用原始官方脚本，全部指标匹配（绝对容差 1e-14）。
- 九个保存模型复现全部 **10,127,700** 条验证预测，逐值精确相等；原生文本模型另验证每组固定 257 条样本。
- 六个新 target 的全部训练行经 NumPy 日期分组、SciPy 平均排名或 NumPy 中位数独立构造，原始/转换标签和行索引指纹通过。
- 同折三组 split、purge、行数、键、原始标签、flags、147 列、预处理、模型参数及实际原生参数（含 32 线程）完全一致。
- Python/NumPy/pandas/LightGBM 和完整依赖锁一致；训练路径源码指纹一致。报告生成和换行恢复工具在训练期间完善，未参与训练计算。
- 输入数据、Full147 矩阵/键/标签缓存、冻结配置、官方脚本及受保护历史结果指纹均通过，未改动 E003 产物。
- 详细逐组证据见 `e003_e004_e005_comparison.json` 的 verification，执行输出见 `e004_e005_reproduction.log`。
- 提交前核对 18 个已暂存逐日/诊断产物的 Git 字节指纹，与六折 result.json 完全一致；六折训练路径源码均可从 Git 暂存字节精确恢复。manifest 文件清单与已跟踪文件一致，大矩阵/模型/预测没有误入 Git。

| 折 | 监督训练行 | 验证行 | purge 日期 |
|---|---:|---:|---|
| F1 | 3,568,878 | 1,125,300 | 2021-12-31 |
| F2 | 4,597,785 | 1,125,300 | 2022-12-30 |
| F3 | 5,659,954 | 1,125,300 | 2023-12-29 |

## 复现边界与源码来源

新结果里的 `git_commit` 记录启动时的基线 HEAD；实际新增训练代码通过每折 `source_sha256` 记录，最终一起提交。未把未提交代码误称为已经存在于该基线提交。

完整训练未重复跑第二遍；固定参数的重复拟合测试使用合成小样本。正式产物的复现采用已保存模型重载并逐值核对全部验证预测，以及原始官方脚本复算。

大矩阵、模型、压缩预测沿用仓库规则保存在本地，Git 提交结果、指纹、逐日指标、代码与报告。完整复现所需本地输入与命令见 `docs/TARGET_COMPARISON.md`。

历史基线部分 YAML、说明/报告和 `baselines.py` 工作区使用 CRLF，而 Git 对应 blob 使用 LF；已有冻结指纹按工作区原始字节计算。新增 `restore_text_contract` 先验证所有候选，仅允许恢复与既有 SHA-256 完全一致的 LF/CRLF 字节，不接受内容变化、不更改冻结指纹。默认只检查；新检出目录必要时显式加 `--restore`。本次当前工作区无需恢复。大文件仍须按原仓库规则另备，不声称仅 Git clone 即可全量复现。

隔离检出模拟：61 个文本文件均通过检查；将副本统一为 LF 后，51 个文件恢复到历史接受指纹，最终 61/61 全部匹配。原工作区未写入，结果见 `e004_e005_text_roundtrip.json`；当前工作区只读检查见 `e004_e005_text_contract_check.json`。
